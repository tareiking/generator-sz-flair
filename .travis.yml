language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-sz-flair' ]; then cd .. && eval "mv $currentfolder generator-sz-flair" && cd generator-sz-flair; fi

